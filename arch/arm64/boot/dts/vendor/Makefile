vendor := $(srctree)/$(src)

ifneq "$(wildcard $(vendor)/qcom)" ""
	subdir-y += qcom
endif

ifneq "$(wildcard $(vendor)/fih)" ""
	subdir-y += fih
endif

ifneq "$(wildcard $(vendor)/longcheer)" ""
	subdir-y += longcheer
endif
